import{_ as s,o as t,c as a,a2 as i}from"./chunks/framework.76ZGnlPi.js";const u=JSON.parse('{"title":"Manual Bank Transfer — Admin Operations","description":"","frontmatter":{},"headers":[],"relativePath":"admin/manual-transfer/index.md","filePath":"admin/manual-transfer/index.md"}'),n={name:"admin/manual-transfer/index.md"};function o(d,e,r,l,h,p){return t(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="manual-bank-transfer-—-admin-operations" tabindex="-1">Manual Bank Transfer — Admin Operations <a class="header-anchor" href="#manual-bank-transfer-—-admin-operations" aria-label="Permalink to &quot;Manual Bank Transfer — Admin Operations&quot;">​</a></h1><p>Admin-only guide for managing the manual bank transfer payment method: destination bank accounts, proof review, approvals, and rejections.</p><blockquote><p><strong>Scope:</strong> Manual bank transfer is available <strong>only for invoices linked to a Custom Booking</strong> (<code>Invoice.customBooking</code>). Regular OTA bookings, wallet top-ups, and ad-hoc invoices are excluded so the OTA ticketing pipeline settles through Xendit without admin intervention. This is enforced both in <code>GET /invoice/:id/available-gateways</code> (gateway hidden) and <code>POST /invoice/:id/generate-payment</code> (returns <code>400 Bad Request</code> if attempted against a non-custom-booking invoice).</p></blockquote><p>All endpoints require:</p><ul><li><code>Authorization: Bearer &lt;jwt&gt;</code> with a user that has the <code>ADMIN</code> role.</li><li>Base path: <code>/api/v1/admin/manual-transfer/*</code></li></ul><blockquote><p><strong>Permission-gated (Access Roles).</strong> Each route also requires a fine permission: proof and bank-account reads need <code>manual-transfers:read</code>, proof approve and the payment reopen need <code>manual-transfers:approve</code>, proof reject needs <code>manual-transfers:reject</code>, and destination bank-account writes (create / update / toggle-active / delete) need <code>manual-transfers:manage</code>. See <a href="/ahlaan-train-docs/admin/roles/">Access Roles</a>.</p></blockquote><hr><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p>The manual transfer feature has two concerns:</p><ol><li><strong>Bank Account Management</strong> — which beneficiary accounts are shown to customers.</li><li><strong>Proof Review</strong> — inspecting uploaded receipts and approving or rejecting them.</li></ol><p>Approvals go through the <strong>same settlement path</strong> as Xendit webhooks (<code>PaymentSettlementService.settlePayment</code>). This means a single approval transitions the invoice, booking, confirmation email, WhatsApp, and ticketing job in one atomic flow. You do <strong>not</strong> need to manually trigger any of these downstream steps.</p><hr><h2 id="sar-has-two-rails-mitra-10" tabindex="-1">SAR has two rails (MITRA-10) <a class="header-anchor" href="#sar-has-two-rails-mitra-10" aria-label="Permalink to &quot;SAR has two rails (MITRA-10)&quot;">​</a></h2><p>A <strong>SAR</strong> custom-booking invoice can be settled <strong>two ways</strong>. USD is unchanged (manual transfer only); IDR is unchanged (Xendit).</p><table tabindex="0"><thead><tr><th>Rail</th><th>Actor</th><th>How</th></tr></thead><tbody><tr><td><strong>Manual transfer</strong> (unchanged)</td><td>Customer uploads a transfer proof → admin approves</td><td>The proof-review flow documented below.</td></tr><tr><td><strong>In-person office settlement</strong> (NEW)</td><td>Admin records a SAR payment settled in person at the Ahlaan office</td><td><code>POST /api/v1/admin/invoice/:id/office-settlement</code> (see below).</td></tr></tbody></table><p>Both rails produce the <strong>same</strong> durable outcome — invoice <code>PAID</code> via the SAR anchor, booking advanced to <code>WAITING_FOR_TICKET</code>, ticketing + confirmation notifications fired — because both run through the shared <code>settlePayment</code> routine.</p><h3 id="post-api-v1-admin-invoice-id-office-settlement" tabindex="-1"><code>POST /api/v1/admin/invoice/:id/office-settlement</code> <a class="header-anchor" href="#post-api-v1-admin-invoice-id-office-settlement" aria-label="Permalink to &quot;\`POST /api/v1/admin/invoice/:id/office-settlement\`&quot;">​</a></h3><p>Admin-only. Records an in-person SAR office settlement against a <strong>custom-booking</strong> invoice. Accepts <strong><code>multipart/form-data</code></strong> so an optional receipt file can be attached.</p><p>The endpoint resolves the payment to settle in <strong>three modes</strong> (precedence order — explicit id, then auto-reuse, then mint):</p><table tabindex="0"><thead><tr><th>Mode</th><th>When to use</th><th>Behaviour</th></tr></thead><tbody><tr><td><strong>Settle existing</strong> (<code>paymentId</code> supplied)</td><td>The admin has the exact payment id in hand (e.g. from the customer rail).</td><td>Settles that payment in place. It must belong to this invoice, be on the <code>IN_PERSON</code> gateway, and still be <code>PENDING</code>. <code>amountSAR</code> is <strong>ignored</strong> — the SAR anchor is recorded.</td></tr><tr><td><strong>Auto-reuse</strong> (<code>paymentId</code> omitted, an open row exists)</td><td><strong>The normal counter flow.</strong> The admin settles from the invoice screen with no payment id, and the customer already started an in-person payment on the <a href="./../../customer/in-person-office-payment/">in-person rail</a>.</td><td>Looks up the customer&#39;s own open <code>PENDING</code> <code>IN_PERSON</code> row (newest first) and settles <strong>that</strong> — no twin is minted. <code>amountSAR</code> is <strong>ignored</strong>; the SAR anchor is recorded.</td></tr><tr><td><strong>Mint and settle</strong> (<code>paymentId</code> omitted, no open row)</td><td>A genuine walk-in the customer never started online.</td><td>Creates a fresh <code>IN_PERSON</code> / <code>CASH</code> Payment and settles it immediately. Unchanged original behaviour.</td></tr></tbody></table><blockquote><p><strong>Auto-reuse fixes the old stranded-<code>PENDING</code> caveat.</strong> No admin read surface emits the customer&#39;s payment id, so the admin settling from the invoice screen never has one. The empty-body path used to always <em>mint</em> a second attempt, leaving the customer&#39;s original row <code>PENDING</code> forever — their instruction page kept telling them to pay an already-settled invoice. Now the empty body reuses that open row instead, so the customer&#39;s own payment is the one that settles. Rows stranded <em>before</em> this change are cleaned up by <a href="./../invoice/#_12-reconcile-orphan-in-person-payments-maintenance"><code>POST .../invoice/reconcile-orphan-in-person</code></a>.</p></blockquote><div class="language-jsonc vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">jsonc</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// multipart/form-data — all parts optional</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;paymentId&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;019f776e-221a-75cc-b67a-cb2928f60c89&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// settle the customer&#39;s existing payment</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;amountSAR&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">5000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// defaults to the full SAR anchor (Invoice.baseTotalAmountSAR); ignored when paymentId is set</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;receiptRef&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;RCP-2026-0142&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;notes&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Settled at Jeddah office counter&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;file&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&lt;binary&gt;&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          // optional receipt: JPG/PNG/WebP/PDF, ≤ 5 MB</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>201</code> returns the settled payment. Real response from a 930 SAR settlement on invoice <code>INV-202607-0030-RFB</code>:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;paymentId&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;019f776f-1641-722e-bb51-8132727c4a41&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;externalId&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;inv-INV-202607-0030-RFB-in_person-2&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;gatewayCode&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;IN_PERSON&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;paymentMethod&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;CASH&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;amount&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">930</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;currency&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;SAR&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;status&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;PAID&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;qrString&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;paymentUrl&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;expiresAt&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;2026-07-18T22:53:16.732Z&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;expiryDisplay&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;iso&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;2026-07-18T22:53:16.732Z&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;local&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;7/18/2026, 10:53:16 PM&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;minutesRemaining&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;message&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Payment must be completed within 0 minutes&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;attemptNumber&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>In <strong>settle-existing</strong> mode the returned <code>paymentId</code>, <code>externalId</code> and <code>attemptNumber</code> are the customer&#39;s original payment rather than a new attempt — that is the whole point of passing <code>paymentId</code>. The example above is mint mode, which is why <code>attemptNumber</code> is <code>2</code>.</p><p>A second call against an already-settled invoice is rejected:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;statusCode&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">400</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;error&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Bad Request&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;message&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Invoice is already settled.&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;code&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;UNCODED&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;category&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;VALIDATION&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;retryable&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><ul><li><strong>Gated by <code>OFFICE_SETTLEMENT_ENABLED</code></strong> (off by default) — returns <code>400</code> with <code>&quot;Office settlement is currently disabled.&quot;</code> when disabled (verified live on preprod; the flag check runs <strong>before</strong> the invoice is loaded). See <a href="./../system-config/">System Config</a>.</li><li><code>400</code> if the invoice is already settled, is not a custom-booking invoice, or the <code>IN_PERSON</code> gateway row is missing/inactive.</li><li>The settlement is a single <strong>full</strong> settlement: the invoice is marked fully <code>PAID</code> regardless of the numeric <code>amountSAR</code> (the SAR anchor is authoritative). Partial/installment office settlements are out of scope.</li><li><strong>Optional proof <code>file</code></strong> — a receipt image or PDF (<code>image/jpeg</code>, <code>image/png</code>, <code>image/webp</code>, <code>application/pdf</code>; ≤ 5 MB, the same allowlist as customer proofs). Stored <strong>privately</strong> in R2; pointers persisted on the <code>Payment</code> (<code>proofFileId</code> / <code>proofFileMimeType</code> / <code>proofFileSizeBytes</code>). Upload happens <strong>before</strong> settlement, so a storage failure returns <code>400 &quot;Failed to store settlement proof.&quot;</code> and leaves <strong>no settled orphan</strong>; an invalid/oversized/wrong-type file is rejected up front before any Payment row is created. Admins retrieve it as a signed URL via the <code>officeSettlementProof</code> block on <code>GET /api/v1/admin/invoice/:id</code> (see the <a href="./../invoice/">Invoice admin guide</a>).</li><li>Records an <code>AuditLog</code> (<code>action=&#39;OFFICE_SETTLEMENT&#39;</code>, <code>entity=&#39;Invoice&#39;</code>, <code>newValue.proofFileId</code> when a file was attached) capturing the settling admin, amount, and <code>receiptRef</code>, alongside the settlement <code>WebhookLog</code> (<code>OfficeSettlement</code> / <code>payment.office_settlement</code>).</li></ul><hr><h2 id="custom-booking-payment-expiry-new" tabindex="-1">Custom-booking payment expiry (new) <a class="header-anchor" href="#custom-booking-payment-expiry-new" aria-label="Permalink to &quot;Custom-booking payment expiry (new)&quot;">​</a></h2><p>Custom bookings now have a <strong>write-side</strong> payment expiry:</p><table tabindex="0"><thead><tr><th>Rail</th><th>Before</th><th>Now</th></tr></thead><tbody><tr><td><strong>Xendit</strong></td><td>Expired, but only via its own <code>payment.expired</code> webhook. A lost callback stranded the booking forever.</td><td>Unchanged in the happy path. A delayed safety-net job fires at <code>expiresAt + grace</code> (default 10 min) if the webhook never arrives, and logs a <strong>warning</strong> so a lost callback is finally visible.</td></tr><tr><td><strong>Manual transfer</strong></td><td>Never expired in the database. <code>mapProofStatus</code> derived &quot;expired&quot; at read time only, so the API said expired while the row stayed <code>WAITING_FOR_PAYMENT</code> forever.</td><td>Expires at 30 minutes: payment <code>EXPIRED</code>, booking <code>EXPIRED</code>, invoice <code>ISSUED -&gt; OVERDUE</code>.</td></tr><tr><td><strong>In-person office</strong></td><td>n/a (new rail)</td><td>Expires at 60 minutes, same three writes.</td></tr></tbody></table><blockquote><p><strong>Off by default.</strong> Gated on <code>CUSTOM_BOOKING_PAYMENT_EXPIRY_JOB_ENABLED</code> (default <code>false</code>); only the literal string <code>true</code> enables it. It ships dark because manual transfer is already live: switching it on means bookings that used to sit in <code>WAITING_FOR_PAYMENT</code> indefinitely will start moving to <code>EXPIRED</code>. Turn it on deliberately and watch.</p></blockquote><p>The invoice is marked <strong><code>OVERDUE</code></strong>, not a new <code>EXPIRED</code> status. <code>OVERDUE</code> already existed with correct readers (it counts in the pending bucket) and, critically, is <strong>not</strong> a settled status, so a reopened payment can still be paid without any extra unblocking step.</p><p><strong>Recovering an expired booking</strong> is the existing two-step counter flow:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">POST</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /api/v1/admin/manual-transfer/payments/:paymentId/reopen</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">POST</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /api/v1/admin/invoice/:id/office-settlement</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">   {</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;paymentId&quot;:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;...&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> }</span></span></code></pre></div><p>Reopen reverses all three writes: payment back to <code>PENDING</code> with a fresh window, booking <code>EXPIRED -&gt; WAITING_FOR_PAYMENT</code>, invoice <code>OVERDUE -&gt; ISSUED</code>.</p><h3 id="what-is-deliberately-not-expired" tabindex="-1">What is deliberately NOT expired <a class="header-anchor" href="#what-is-deliberately-not-expired" aria-label="Permalink to &quot;What is deliberately NOT expired&quot;">​</a></h3><ul><li><strong>A booking whose proof is under review.</strong> Only <code>WAITING_FOR_PAYMENT</code> is expired, never <code>WAITING_PAYMENT_REVIEW</code>. A submitted proof sitting in the admin queue is us being slow, not the customer failing to pay - and since <code>EXPIRED</code> is terminal and settlement only advances a booking that is still awaiting payment or review, expiring it would have produced a <code>PAID</code> invoice against a permanently dead booking.</li><li><strong>A booking whose invoice still has another open payment.</strong> A customer who generated a Xendit link and then switched to the office rail has two payments with different windows. The shorter one lapsing must not kill a booking the customer is still on their way to pay for.</li></ul><h3 id="paying-an-expired-booking" tabindex="-1">Paying an expired booking <a class="header-anchor" href="#paying-an-expired-booking" aria-label="Permalink to &quot;Paying an expired booking&quot;">​</a></h3><p>Once a booking is <code>EXPIRED</code>, payment and settlement are <strong>refused</strong>. That is deliberate: without the guard a customer could pay a second time, the proof would look normal in the admin queue, approval would mark the invoice <code>PAID</code>, and the booking would stay <code>EXPIRED</code> forever with the money already taken.</p><p>Reopen is the route back. It reverses booking <code>EXPIRED -&gt; WAITING_FOR_PAYMENT</code> and invoice <code>OVERDUE -&gt; ISSUED</code>, and it does so whatever state the payment itself is in.</p><blockquote><p><strong>Known gap.</strong> A custom booking that expired on a <strong>XENDIT-only</strong> invoice has no reopen path today, because reopen deliberately refuses XENDIT (reopening it would extend our window while Xendit&#39;s own invoice stays dead). Such a booking is blocked rather than payable - the safer failure, but it needs a rail-independent admin &quot;reopen booking&quot; action to resolve properly.</p></blockquote><h3 id="known-accepted-race-xendit-only" tabindex="-1">Known accepted race (Xendit only) <a class="header-anchor" href="#known-accepted-race-xendit-only" aria-label="Permalink to &quot;Known accepted race (Xendit only)&quot;">​</a></h3><p>If a Xendit <code>paid</code> webhook lands <strong>after</strong> <code>expiresAt + grace</code>, the job has already expired the booking and marked the invoice <code>OVERDUE</code>. The late webhook still settles, because settlement&#39;s compare-and-set is <code>status: { not: PAID }</code> and <code>OVERDUE</code> is deliberately not a settled status. Net result: a <strong><code>PAID</code> invoice against an <code>EXPIRED</code> booking</strong>.</p><p>This is a state mismatch, not money loss: the payment is captured and the invoice is correct; only the booking status disagrees. The admin reopen flow is the repair. If the safety-net warning starts firing on healthy webhooks, the grace is tuned too tight - raise <code>CUSTOM_BOOKING_EXPIRY_SAFETY_GRACE_MINUTES</code> (config, no code change).</p><hr><h2 id="bank-account-management" tabindex="-1">Bank Account Management <a class="header-anchor" href="#bank-account-management" aria-label="Permalink to &quot;Bank Account Management&quot;">​</a></h2><h3 id="create" tabindex="-1">Create <a class="header-anchor" href="#create" aria-label="Permalink to &quot;Create&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">POST</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /api/v1/admin/manual-transfer/bank-accounts</span></span></code></pre></div><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;bankCode&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;BCA&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;bankName&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Bank Central Asia&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;accountNumber&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;1234567890&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;accountName&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;PT Haramain&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;currency&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;IDR&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;priority&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">100</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;notes&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Primary account&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><ul><li><code>priority</code> (higher = preferred). Default-selection logic in <code>generate-payment</code> picks the highest-priority <strong>active</strong> account for the <strong>chosen payment currency</strong>.</li><li><code>isActive</code> defaults to <code>true</code>.</li><li><code>currency</code> is the routing key. Manual transfer now supports <strong>SAR and USD</strong> for custom bookings (in addition to IDR), and <strong>each currency routes to its own account</strong>. To enable the multi-currency flow, seed one active account per foreign currency, e.g.: <ul><li><strong>SAR</strong> → Bank Mandiri (<code>{&quot;bankCode&quot;:&quot;MANDIRI&quot;,&quot;currency&quot;:&quot;SAR&quot;, ...}</code>)</li><li><strong>USD</strong> → Bank CIMB Niaga (<code>{&quot;bankCode&quot;:&quot;CIMB&quot;,&quot;currency&quot;:&quot;USD&quot;, ...}</code>) The customer is told to transfer the SAR/USD amount to the matching account. <code>GET /invoice/:id/payment-options</code> surfaces exactly the currencies that have an active account — a currency with none is hidden from the FE note. (<code>bankCode</code> must already exist in <code>/admin/banks</code>.)</li></ul></li><li><strong>Reconciliation suffix:</strong> the unique 3-digit suffix applies to <strong>IDR only</strong> (whole-rupiah). SAR/USD proofs are stored with <code>amountSuffix=0</code> and the exact decimal amount, so admins match the receipt by amount + sender directly.</li></ul><h3 id="list-get-update-toggle-delete" tabindex="-1">List / Get / Update / Toggle / Delete <a class="header-anchor" href="#list-get-update-toggle-delete" aria-label="Permalink to &quot;List / Get / Update / Toggle / Delete&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Verb + Path</th><th>Purpose</th></tr></thead><tbody><tr><td><code>GET /admin/manual-transfer/bank-accounts</code></td><td>List (filter by <code>currency</code>, <code>isActive</code>)</td></tr><tr><td><code>GET /admin/manual-transfer/bank-accounts/:id</code></td><td>Detail</td></tr><tr><td><code>PATCH /admin/manual-transfer/bank-accounts/:id</code></td><td>Update mutable fields</td></tr><tr><td><code>PATCH /admin/manual-transfer/bank-accounts/:id/toggle-active</code></td><td>Flip <code>isActive</code></td></tr><tr><td><code>DELETE /admin/manual-transfer/bank-accounts/:id</code></td><td>Soft-delete if proofs reference it; hard-delete otherwise</td></tr></tbody></table><blockquote><p><strong>Business rules:</strong></p><ul><li><code>MANUAL_TRANSFER</code> is automatically hidden from customers when no active bank account exists for the invoice currency. Toggle-off the last active account in a currency and the gateway disappears end-to-end — useful when you need a short maintenance window.</li><li><code>MANUAL_TRANSFER</code> is also hidden for any invoice <strong>not</strong> linked to a Custom Booking, regardless of bank-account configuration. Regular OTA booking invoices, wallet top-ups, and ad-hoc invoices will never surface the gateway — customers on those flows must pay via Xendit.</li></ul></blockquote><hr><h2 id="proof-review" tabindex="-1">Proof Review <a class="header-anchor" href="#proof-review" aria-label="Permalink to &quot;Proof Review&quot;">​</a></h2><h3 id="list-proofs" tabindex="-1">List Proofs <a class="header-anchor" href="#list-proofs" aria-label="Permalink to &quot;List Proofs&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">GET</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /api/v1/admin/manual-transfer/proofs</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  ?status</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">=SUBMITTED</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &amp;bankAccountId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">01925a4b-...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &amp;invoiceNumber</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">INV-2026-0420</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &amp;bookingCode</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">XVW3NR</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  &amp;page</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&amp;pageSize</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">20</span></span></code></pre></div><h3 id="get-proof-with-signed-download-url" tabindex="-1">Get Proof (with signed download URL) <a class="header-anchor" href="#get-proof-with-signed-download-url" aria-label="Permalink to &quot;Get Proof (with signed download URL)&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">GET</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /api/v1/admin/manual-transfer/proofs/:id</span></span></code></pre></div><p>The response includes <code>signedDownloadUrl</code> — a signed URL (TTL: <strong>24 hours</strong>) to download the uploaded receipt from private R2 storage. The longer TTL reflects real-world admin workflows (URLs copied into internal tickets, Slack threads, email handoffs). The underlying file remains stored <strong>private</strong> in R2 — only the signed URL is shareable and it still expires. Do <strong>not</strong> paste the URL into any customer-facing channel.</p><p>The same <code>signedDownloadUrl</code> (and the full review block) is also embedded in the <code>manualTransfer</code> field of:</p><ul><li><code>GET /api/v1/admin/invoice/:id</code></li><li><code>GET /api/v1/admin/custom-booking/:id</code></li></ul><p>so reviewers don&#39;t need to cross-navigate to the dedicated proofs page for routine approvals.</p><h3 id="partner-identity-on-proofs" tabindex="-1">Partner identity on proofs (MITRA-5/7) <a class="header-anchor" href="#partner-identity-on-proofs" aria-label="Permalink to &quot;Partner identity on proofs (MITRA-5/7) {#partner-identity-on-proofs}&quot;">​</a></h3><p>Both the proof <strong>list</strong> and <strong>detail</strong> payloads nest the owning invoice under <code>payment.invoice</code>, which carries the <strong>mitra partner snapshot</strong> so finance can reconcile which partner a transfer belongs to without a second lookup:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;payment&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;019f4370-edbc-739a-bd22-fa0d51c221c0&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;status&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;PENDING&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;amount&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1860</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;currency&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;SAR&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;invoice&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;invoiceNumber&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;INV-202607-0003-9QJ&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;partnerRegistrationNumber&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;partnerName&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;currency&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;IDR&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;status&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ISSUED&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>partnerRegistrationNumber</code> (<code>MS.XXX</code> / <code>MG.XXX</code>) and <code>partnerName</code> are the same snapshot documented on the <a href="/ahlaan-train-docs/admin/invoice/#partner">invoice <code>partner</code> block</a> — so both are <strong><code>null</code> for a non-mitra invoice</strong> (as in the live example above, a standard invoice with no partner snapshot). They are only populated on custom-booking (promo-surface) invoices.</p><h3 id="approve" tabindex="-1">Approve <a class="header-anchor" href="#approve" aria-label="Permalink to &quot;Approve&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">POST</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /api/v1/admin/manual-transfer/proofs/:id/approve</span></span></code></pre></div><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;adminNotes&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Amount matches; transfer dated 2026-04-20&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;paidAt&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;2026-04-20T13:05:00.000Z&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Both fields optional. <code>paidAt</code> defaults to &quot;now&quot; if omitted.</p><p><strong>What happens on success:</strong></p><ol><li>Proof → <code>APPROVED</code>, <code>reviewedAt</code> + <code>reviewerId</code> populated.</li><li><code>PaymentSettlementService.settlePayment</code> runs with <code>trigger: &#39;MANUAL_TRANSFER_APPROVAL&#39;</code>, which: <ul><li>Marks <code>Payment.status = PAID</code>.</li><li>Recomputes <code>Invoice.status</code> (<code>PAID</code> / <code>PARTIALLY_PAID</code> / <code>OVERPAID</code>) — transitioning out of <code>WAITING_PAYMENT_REVIEW</code>. For a <strong>cross-currency</strong> payment (SAR/USD payment on an IDR invoice) the invoice is marked <code>PAID</code> via the SAR anchor (<code>totalPaidSAR = baseTotalAmountSAR</code>), and the invoice-currency <code>totalPaid</code> is <strong>derived from that anchor</strong>: <code>totalPaid = totalAmount × (totalPaidSAR / baseTotalAmountSAR)</code>, which equals <code>totalAmount</code> on a full pay and is capped there. The foreign amount is never <em>summed</em> onto the invoice-currency column — the actual SAR/USD figure received stays on the <code>Payment</code> row. Duplicate detection is <strong>status-aware</strong>: approving a second foreign-currency proof on an already-settled invoice records it as <code>OVERPAID</code> (and queues the duplicate-payment notice) <strong>without</strong> re-advancing the booking or mixing currencies.</li><li><strong>Why derived, not converted.</strong> <code>totalPaid</code> comes from the SAR anchor rather than <code>SAR × sellRate</code> because a GOLD booking is priced at the <em>reduced</em> rate — converting at the base sell rate would overstate the total and flip the invoice to a false <code>OVERPAID</code>.</li><li><strong>Cross-currency is detected structurally, from the data.</strong> A payment is cross-currency when its currency differs from the invoice currency (<code>payment.currency !== invoice.currency</code>, case-insensitive) — <strong>not</strong> via a payment-gateway allowlist, and not by which admin endpoint ran settlement. Foreign rails (<code>MANUAL_TRANSFER</code> SAR/USD proof, <code>IN_PERSON</code> SAR office settlement) are the usual source, but keying off the gateway previously let a foreign amount slip onto the invoice-currency total whenever the gateway relation was absent or mislabeled. So the generic <a href="./../payment/#_2-manual-confirm-payment"><code>POST /admin/payment/:id/manual-confirm</code></a> stuck-payment fix (trigger <code>MANUAL_ADMIN</code>) and the in-person office settlement (trigger <code>OFFICE_SETTLEMENT</code>) settle a foreign amount <strong>identically</strong> to this proofs/approve path. When the payment currency is unknown it falls back to the invoice currency and is treated as same-currency (the safe default: sum, do not strand).</li><li>Transitions any linked <code>CustomBooking.status</code> from <code>WAITING_FOR_PAYMENT</code> <strong>or</strong> <code>WAITING_PAYMENT_REVIEW</code> → <code>WAITING_FOR_TICKET</code> (skip-PAID; guarded by <code>updateMany</code> scoped to those two states, so bookings already advanced are left untouched).</li><li>Transitions <code>Booking.status</code> to <code>CONFIRMED</code> and inserts <code>BookingStatusHistory</code>.</li><li>Queues booking confirmation email + WhatsApp (if the booking has <code>otaTimeLimits.updateTravellersTimeLimit</code>).</li><li>Cancels any pending booking-expiration jobs.</li><li>Queues OTA ticketing unless the payment is a duplicate.</li><li>Writes a <code>WebhookLog</code> entry with <code>webhookType=&#39;ManualTransferApproval&#39;</code> so the audit trail matches the Xendit path.</li></ul></li></ol><p><strong>If settlement fails</strong>, the approval call throws and the proof stays <code>SUBMITTED</code>. The admin can retry once the underlying cause is resolved. Approval runs settlement first and only then flips the proof to <code>APPROVED</code> via a <code>status=SUBMITTED</code> guarded <code>updateMany</code> — a concurrent second approval on the same proof is a logged no-op (payment remains <code>PAID</code>).</p><h3 id="reject" tabindex="-1">Reject <a class="header-anchor" href="#reject" aria-label="Permalink to &quot;Reject&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">POST</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /api/v1/admin/manual-transfer/proofs/:id/reject</span></span></code></pre></div><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;reason&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Amount transferred does not match the unique total (expected 1,250,037 IDR; received 1,250,000 IDR)&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>reason</code> is <strong>required</strong>.</p><p><strong>What happens:</strong></p><ol><li><code>retryCount</code> is incremented atomically.</li><li>Proof → <code>REJECTED</code>, <code>rejectionReason</code> populated.</li><li>If <code>retryCount &gt; maxRetryCount</code> (default 3 re-uploads allowed after the initial submission — i.e. the 4th rejection), <code>Payment.status = FAILED</code>; otherwise payment stays <code>PENDING</code> and the customer may re-upload.</li><li><code>Invoice.status</code> is reverted from <code>WAITING_PAYMENT_REVIEW → ISSUED</code> and any linked <code>CustomBooking.status</code> is reverted from <code>WAITING_PAYMENT_REVIEW → WAITING_FOR_PAYMENT</code> (guarded by <code>updateMany</code> so a concurrently-settled invoice is not clobbered). This happens for <strong>both</strong> non-final and final rejections, so admins see a clean &quot;payment failed, awaiting customer action&quot; state either way.</li><li>A rejection email is queued to the invoice user (or guest email).</li></ol><blockquote><p><strong>Authoring rejection reasons:</strong> Be specific. The FE surfaces this text verbatim in a banner, so &quot;amount mismatch — transfer IDR 1,250,037 exactly&quot; is far more actionable than &quot;wrong amount&quot;.</p></blockquote><h3 id="reopen-for-re-upload" tabindex="-1">Reopen for re-upload <a class="header-anchor" href="#reopen-for-re-upload" aria-label="Permalink to &quot;Reopen for re-upload&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">POST</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /api/v1/admin/manual-transfer/payments/:paymentId/reopen</span></span></code></pre></div><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;addMinutes&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">120</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;resetRetries&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;reason&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Customer transferred late; granting 2h to upload proof.&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Note the path is keyed on <strong><code>paymentId</code></strong> (a payment-level operation), not the proof id used by approve/reject.</p><p>Re-enables proof upload for a payment that the customer-facing guard would otherwise block. That guard rejects an upload when the payment is not <code>PENDING</code>, when <code>expiresAt</code> is in the past, or when the re-upload budget is exhausted (<code>retryCount &gt; maxRetryCount</code>). Reopen clears these so the admin can grant another attempt <strong>without</strong> the customer starting a new payment.</p><table tabindex="0"><thead><tr><th>Field</th><th>Required</th><th>Effect</th></tr></thead><tbody><tr><td><code>addMinutes</code></td><td>yes</td><td><code>1</code>–<code>43200</code> (30 days). Sets <code>expiresAt = now + addMinutes</code> and flips the payment back to <code>PENDING</code>. The cap is an anti-typo guard, not a policy limit.</td></tr><tr><td><code>resetRetries</code></td><td>no (default <code>false</code>)</td><td>Returns the proof to <code>REJECTED</code> (re-uploadable) and zeroes <code>retryCount</code>. <strong>Required to reopen a <code>FAILED</code> payment</strong> — without it the retry guard would immediately re-block the upload, so the request is rejected with <code>400</code>.</td></tr><tr><td><code>reason</code></td><td>no</td><td>Audit note (≤500 chars), stored on <code>ManualTransferProof.adminNotes</code> and logged.</td></tr></tbody></table><p><strong>What happens:</strong></p><ol><li>Loads the payment&#39;s manual-transfer proof. Missing → <code>404</code>.</li><li><strong>PAID / REFUNDED → <code>409</code></strong> (terminal; cannot reopen).</li><li><strong>FAILED without <code>resetRetries</code> → <code>400</code>.</strong></li><li>A guarded <code>payment.updateMany</code> (CAS on <code>status IN (PENDING, EXPIRED, FAILED)</code>) sets <code>status = PENDING</code>, <code>expiresAt = now + addMinutes</code>. If it matches nothing (the payment transitioned concurrently, e.g. settled to <code>PAID</code>) → <code>409</code>.</li><li>The proof&#39;s <code>adminNotes</code> is stamped with <code>reason</code>; with <code>resetRetries</code>, the proof also goes <code>REJECTED</code> and <code>retryCount → 0</code>.</li><li><strong>EXPIRED only:</strong> the linked <code>CustomBooking.status</code> is reversed <code>EXPIRED → WAITING_FOR_PAYMENT</code> (guarded <code>updateMany</code>). A FAILED reopen needs no booking reversal — <code>rejectProof</code> already left the booking in <code>WAITING_FOR_PAYMENT</code>.</li></ol><blockquote><p><strong>No customer notification.</strong> Reopen sends no email or WhatsApp. Admins coordinate the reopened window with the customer directly.</p></blockquote><hr><h2 id="state-machine" tabindex="-1">State Machine <a class="header-anchor" href="#state-machine" aria-label="Permalink to &quot;State Machine&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>          user uploads             admin approves</span></span>
<span class="line"><span>  AWAITING_UPLOAD  ────────►  SUBMITTED  ────────►  APPROVED  ──► settlement</span></span>
<span class="line"><span>         ▲                         │</span></span>
<span class="line"><span>         │                         │ admin rejects</span></span>
<span class="line"><span>         │                         ▼</span></span>
<span class="line"><span>         │                      REJECTED  ──► if retryCount ≤ max: customer re-uploads → SUBMITTED</span></span>
<span class="line"><span>         │                                  └► if retryCount &gt; max: Payment.status = FAILED</span></span>
<span class="line"><span>         │</span></span>
<span class="line"><span>         └─── (a fresh payment creates a new proof row)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  Admin reopen (POST .../payments/:paymentId/reopen) re-enables upload out-of-band:</span></span>
<span class="line"><span>    Payment PENDING/EXPIRED/FAILED ──► PENDING (expiresAt = now + addMinutes)</span></span>
<span class="line"><span>      EXPIRED  also reverses CustomBooking EXPIRED → WAITING_FOR_PAYMENT</span></span>
<span class="line"><span>      FAILED   requires resetRetries → proof REJECTED, retryCount 0</span></span></code></pre></div><hr><h2 id="operational-guidance" tabindex="-1">Operational Guidance <a class="header-anchor" href="#operational-guidance" aria-label="Permalink to &quot;Operational Guidance&quot;">​</a></h2><ul><li><strong>Verify the amount first.</strong> Cross-reference the <code>uniqueAmount</code> on the instruction payload with the amount shown on the receipt.</li><li><strong>Reconciliation handle depends on the unique-suffix toggle.</strong> The runtime <code>MANUAL_TRANSFER_ENABLE_UNIQUE_SUFFIX</code> flag is managed through the admin system-config API (see &quot;Runtime toggle&quot; below). When <strong>enabled</strong>, the last three digits of the unique total are the reconciliation handle — two concurrent same-amount payments are disambiguated automatically. When <strong>disabled</strong> (the default), <code>uniqueAmount</code> equals the raw base amount and <code>amountSuffix=0</code>, so two concurrent same-amount payments on the same bank account cannot be distinguished by amount alone — always cross-check sender name, receipt timestamp, and booking code as well.</li><li><strong>Verify the sender.</strong> <code>senderAccountName</code> on the proof should reasonably match the name shown on the transfer receipt.</li><li><strong>Verify the destination.</strong> Confirm the receipt&#39;s beneficiary account matches <code>bankAccount.accountNumber</code>.</li><li><strong>Time-box review.</strong> The customer&#39;s payment expires on a fixed window — <strong>30 minutes by default</strong> from payment creation, configurable per environment via <code>CUSTOM_BOOKING_MANUAL_TRANSFER_EXPIRY_MINUTES</code>. The window is enforced at proof <strong>upload</strong> time: once <code>expiresAt</code> passes, a new upload or re-upload is rejected (<code>409</code>, message <code>Payment has expired; please start a new payment.</code>). A proof that was already <strong>submitted</strong> before the window closed stays approvable — admin approval has no <code>expiresAt</code> guard, so a real transfer that landed in time still settles even if review runs past <code>expiresAt</code>. Rejection forcing a re-upload, however, eats into the remaining window. If your review SLA needs longer than the default, raise this env so the window covers transfer → upload end-to-end.</li><li><strong>Duplicate detection is automatic.</strong> If the invoice was already paid (e.g., double-submission via Xendit), <code>settlePayment</code> flags the payment as a duplicate and suppresses re-ticketing — but you should still investigate and refund if needed.</li></ul><hr><h2 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h2><h3 id="environment-variables" tabindex="-1">Environment variables <a class="header-anchor" href="#environment-variables" aria-label="Permalink to &quot;Environment variables&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Env var</th><th>Default</th><th>Purpose</th></tr></thead><tbody><tr><td><code>MANUAL_TRANSFER_MAX_RETRIES</code></td><td><code>3</code></td><td>Max proof re-uploads before the payment is marked <code>FAILED</code> on the next rejection</td></tr><tr><td><code>MANUAL_TRANSFER_SUFFIX_RETRIES</code></td><td><code>10</code></td><td>Max attempts to allocate a unique <code>(bankAccount, uniqueAmount)</code> suffix <strong>when the unique-suffix flag is enabled</strong>. Enforced by a partial unique index on in-flight (<code>AWAITING_UPLOAD</code>/<code>SUBMITTED</code>) proofs with <code>amountSuffix &lt;&gt; 0</code>; on <code>P2002</code> the gateway retries with a fresh random suffix, or throws <code>ConflictException</code> after exhausting retries. Ignored when the unique-suffix flag is disabled.</td></tr><tr><td><code>MANUAL_TRANSFER_ENABLE_UNIQUE_SUFFIX</code></td><td><code>false</code></td><td><strong>Bootstrap seed default only.</strong> Consumed by <code>prisma/seed.ts</code> on first boot to create the <code>MANUAL_TRANSFER_ENABLE_UNIQUE_SUFFIX</code> row in <code>SystemConfiguration</code>. Once the row exists, the live value is controlled by admins at runtime (see below) and re-seeding does not overwrite it.</td></tr></tbody></table><p>Retry-limit env vars take effect on the next request — no restart needed since <code>ConfigService</code> is read on each call.</p><h3 id="runtime-toggle-—-unique-suffix-feature" tabindex="-1">Runtime toggle — unique-suffix feature <a class="header-anchor" href="#runtime-toggle-—-unique-suffix-feature" aria-label="Permalink to &quot;Runtime toggle — unique-suffix feature&quot;">​</a></h3><p>Flip the feature at runtime through the generic system-config API:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">PATCH</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /api/v1/admin/system-config</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  &quot;key&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;MANUAL_TRANSFER_ENABLE_UNIQUE_SUFFIX&quot;,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  &quot;value&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;true&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">   //</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> or</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;false&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><ul><li><strong>Effect is immediate</strong> on the pod that serves the request; other pods converge within the Redis cache TTL (≤5 minutes).</li><li><strong>Existing proofs are unaffected</strong> — the flag only governs new proofs (gateway payment creation) and the <code>REJECTED → SUBMITTED</code> re-upload flip.</li><li>When the feature is <code>false</code>, the partial unique index on <code>(bankAccountId, uniqueAmount)</code> is scoped to <code>amountSuffix &lt;&gt; 0</code>, so multiple zero-suffix in-flight proofs on the same bank account coexist. Reconciliation must fall back to sender + timestamp.</li></ul><p>See <a href="/ahlaan-train-docs/admin/system-config/#manual-transfer-settings">System Configuration Guide → Manual Transfer Settings</a> for the full schema.</p><hr><h2 id="audit-trail" tabindex="-1">Audit Trail <a class="header-anchor" href="#audit-trail" aria-label="Permalink to &quot;Audit Trail&quot;">​</a></h2><ul><li><strong>Every approval</strong> writes a <code>WebhookLog</code> with <code>webhookType=&#39;ManualTransferApproval&#39;</code> and the admin user id. This matches the existing webhook dashboard query so the audit timeline is unified with Xendit events.</li><li><strong>Every rejection</strong> logs via the NestJS <code>Logger</code> with the proof id, retry count, and final-or-not flag.</li><li><strong>Every reopen</strong> logs via the NestJS <code>Logger</code> with the payment id, admin user id, source status, <code>addMinutes</code>, and <code>resetRetries</code>. The <code>reason</code> is additionally persisted to <code>ManualTransferProof.adminNotes</code>.</li></ul><hr><h2 id="related-guides" tabindex="-1">Related Guides <a class="header-anchor" href="#related-guides" aria-label="Permalink to &quot;Related Guides&quot;">​</a></h2><ul><li><a href="/ahlaan-train-docs/admin/payment-gateway/">Payment Gateway Management</a></li><li><a href="/ahlaan-train-docs/customer/manual-transfer/">Customer Manual Transfer Guide</a></li><li><a href="/ahlaan-train-docs/admin/booking-management/">Booking Management</a></li></ul>`,114)])])}const k=s(n,[["render",o]]);export{u as __pageData,k as default};
