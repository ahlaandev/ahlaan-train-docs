import{_ as t,o as s,c as a,a2 as i}from"./chunks/framework.76ZGnlPi.js";const k=JSON.parse('{"title":"Custom Booking Shortfall Settlements (Admin)","description":"","frontmatter":{},"headers":[],"relativePath":"admin/shortfall-settlements/index.md","filePath":"admin/shortfall-settlements/index.md"}'),o={name:"admin/shortfall-settlements/index.md"};function n(d,e,r,l,h,c){return s(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="custom-booking-shortfall-settlements-admin" tabindex="-1">Custom Booking Shortfall Settlements (Admin) <a class="header-anchor" href="#custom-booking-shortfall-settlements-admin" aria-label="Permalink to &quot;Custom Booking Shortfall Settlements (Admin)&quot;">​</a></h1><p>This guide covers the <strong>admin</strong> endpoints that record money returned to a customer for the passengers a ticket source could not ticket, and that move the booking from <code>TICKET_UPLOADED_PARTIAL</code> to <code>SHORTFALL_REFUNDED</code>.</p><p>For the upload that creates the shortfall in the first place, see <a href="/ahlaan-train-docs/admin/custom-booking/#_5-upload-ticket">Custom Booking → §5 Upload Ticket</a>. For bank refunds against a cancelled booking — a different record type on the same screen — see <a href="/ahlaan-train-docs/admin/refunds/">Refund Management</a>.</p><blockquote><p><strong>Permission-gated (Access Roles).</strong> Beyond the <code>ADMIN</code> role, these routes require a fine-grained permission. Creating a settlement needs its own dedicated code, <strong><code>refunds:shortfall-settlement</code></strong> (<code>PERMISSIONS.REFUNDS_SHORTFALL_SETTLEMENT</code>); both reads need <strong><code>refunds:read</code></strong>, the same code that gates bank-refund reads.</p><p><strong>Why its own code, instead of reusing <code>refunds:approve</code>.</strong> Approving a bank refund pays out against a <code>Refund</code> row. This writes the booking into <code>SHORTFALL_REFUNDED</code> — a state no route can leave except by closing or cancelling the booking. That is a different act with a harder-to-undo consequence than approving a refund, so it should be granted deliberately rather than inherited from the refund-approval code.</p><p><strong>No existing role has this code.</strong> It must be granted explicitly before any role can use this endpoint — a <code>refunds:*</code> wildcard holder does inherit it, but no seeded role lists it individually. See <a href="/ahlaan-train-docs/admin/roles/">Access Roles</a>.</p></blockquote><blockquote><p>⛔ <strong>This is the only writer of <code>SHORTFALL_REFUNDED</code>.</strong><code>PATCH /admin/custom-booking/:id/status</code> refuses that value outright with a <code>400</code>, deliberately: a status asserting that money moved must not be settable without a record of the money moving. There is no way to mark a booking settled without creating a settlement row.</p></blockquote><hr><h2 id="quick-overview" tabindex="-1">Quick Overview <a class="header-anchor" href="#quick-overview" aria-label="Permalink to &quot;Quick Overview&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Feature</th><th>Endpoint</th></tr></thead><tbody><tr><td><strong>Record a settlement</strong></td><td><code>POST /api/v1/admin/shortfall-settlements</code></td></tr><tr><td><strong>List settlements</strong></td><td><code>GET /api/v1/admin/shortfall-settlements</code></td></tr><tr><td><strong>Get one settlement</strong></td><td><code>GET /api/v1/admin/shortfall-settlements/:id</code></td></tr><tr><td><strong>Access</strong></td><td>Admin role (<code>ADMIN</code>) <strong>plus</strong> a per-route permission — see below</td></tr></tbody></table><table tabindex="0"><thead><tr><th>Route</th><th>Code</th></tr></thead><tbody><tr><td><code>POST /admin/shortfall-settlements</code></td><td><code>refunds:shortfall-settlement</code></td></tr><tr><td><code>GET /admin/shortfall-settlements</code></td><td><code>refunds:read</code></td></tr><tr><td><code>GET /admin/shortfall-settlements/:id</code></td><td><code>refunds:read</code></td></tr></tbody></table><p><strong>Which wildcards satisfy them.</strong> <code>*</code> and <code>refunds:*</code> confer both codes. <strong><code>*:read</code> confers <code>refunds:read</code> only</strong> — that wildcard matches actions literally named <code>read</code>, so a read-only role can list and inspect settlements but gets <code>403</code> on create. No seeded role carries <code>refunds:shortfall-settlement</code> on its own — it has to be granted before the create route can be used.</p><p><strong>Rate limits.</strong> Create is throttled on the shared <code>file-upload</code> bucket at <strong>10 req / 60s</strong>; both reads are throttled at <strong>60 req / 60s</strong>.</p><hr><h2 id="what-a-shortfall-settlement-is" tabindex="-1">What a shortfall settlement is <a class="header-anchor" href="#what-a-shortfall-settlement-is" aria-label="Permalink to &quot;What a shortfall settlement is&quot;">​</a></h2><p>A ticket source under-delivers. The admin uploads what arrived and records how many passengers it actually covers, and because that count is below the seated total (<code>adultCount + childCount</code>; infants take no seat and are issued no ticket) the booking lands in <strong><code>TICKET_UPLOADED_PARTIAL</code></strong>, optionally carrying a free-text <code>shortfallReason</code> and a supporting attachment. Both are optional, so a booking can reach this status with no explanation recorded at all.</p><p>The customer has paid for passengers who were never ticketed. Finance transfers that money back by hand, outside this system. <strong>This endpoint is the record of that transfer.</strong> It writes a settlement row, moves the booking to <strong><code>SHORTFALL_REFUNDED</code></strong>, and writes an audit entry naming the admin who did it.</p><p><code>TICKET_UPLOADED_PARTIAL</code> is the <strong>only</strong> status a settlement can be created from — it is the one status with a shortfall outstanding. Every other status either has nothing owed or has already dealt with it, and <code>COMPLETED</code> in particular has no outgoing transition, so settling there would leave a closed booking asserting that money moved with no way to reflect it.</p><hr><h2 id="_1-record-a-shortfall-settlement" tabindex="-1">1. Record a Shortfall Settlement <a class="header-anchor" href="#_1-record-a-shortfall-settlement" aria-label="Permalink to &quot;1. Record a Shortfall Settlement&quot;">​</a></h2><div class="language-http vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">http</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">POST</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /api/v1/admin/shortfall-settlements</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">Authorization</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Bearer &lt;admin-token&gt;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">Content-Type</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> multipart/form-data</span></span></code></pre></div><p>Multipart, because the proof of transfer rides along in the same request.</p><h3 id="body-fields" tabindex="-1">Body Fields <a class="header-anchor" href="#body-fields" aria-label="Permalink to &quot;Body Fields&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Required</th><th>Notes</th></tr></thead><tbody><tr><td><code>customBookingId</code></td><td>uuid</td><td>Yes</td><td>The booking whose shortfall is being settled. Must currently be in <code>TICKET_UPLOADED_PARTIAL</code>.</td></tr><tr><td><code>shortfallAmountSAR</code></td><td>number</td><td>Yes</td><td>What the customer is owed, in SAR. <strong>The canonical accounting figure</strong> — every report and total reads it, and the settlement leg only describes how it physically moved. Max 2 decimal places, must be positive.</td></tr><tr><td><code>settlementCurrency</code></td><td>enum</td><td>Yes</td><td><code>SAR</code>, <code>USD</code> or <code>IDR</code>. The currency actually transferred.</td></tr><tr><td><code>settlementAmount</code></td><td>number</td><td>Conditional</td><td>Amount transferred, in <code>settlementCurrency</code>. Max 2dp, positive. Must be sent <strong>together with</strong> <code>settlementRateToSAR</code> — see <a href="#the-currency-contract">The currency contract</a>.</td></tr><tr><td><code>settlementRateToSAR</code></td><td>number</td><td>Conditional</td><td><strong>SAR per ONE unit of <code>settlementCurrency</code>.</strong> Stored at 8dp, but the smallest value you can SEND is <code>0.000001</code> — anything below that is written in exponential notation by the time it is validated and is rejected. Ample for every supported currency (IDR settles near <code>0.00024</code>). Upper bound <code>9999999999</code>. Same pairing rule.</td></tr><tr><td><code>reference</code></td><td>string</td><td>Yes</td><td>Bank transfer reference for the money that moved. Free text, max 255 chars, must not be empty. <strong>The only handle finance has on the transaction</strong> — the settlement record stores no destination account.</td></tr><tr><td><code>amountReason</code></td><td>string</td><td>No</td><td>How the figure was reached — bank fee absorbed, goodwill, rounding, which passengers. Max 2000 chars. The <strong>only</strong> explanation the record carries.</td></tr><tr><td><code>proofFile</code></td><td>file</td><td>No</td><td>Proof of the transfer — normally a bank receipt or a screenshot.</td></tr></tbody></table><p><strong><code>proofFile</code> accepted types:</strong> <code>application/pdf</code>, <code>image/jpeg</code>, <code>image/png</code>. Anything else answers <code>400</code> naming the rejected type. The file is stored in R2 with <code>entityType: custom_booking_shortfall_proof</code>, <code>visibility: PRIVATE</code>, attached to the <strong>booking</strong> — the customer never sees it.</p><p><strong>It cannot be deleted, including by an admin.</strong> <code>DELETE /storage/:fileId</code> answers <code>403</code> for <code>custom_booking_shortfall_proof</code>, for <code>custom_booking_shortfall_attachment</code> and for the retired <code>custom_booking_shortfall_report</code>: each is evidence behind a refund, that route carries no permission check and writes no audit row, and the R2 delete is irrecoverable. Correct a wrong file by uploading a replacement. The rollback path is unaffected — a settlement whose status write loses the race still has its orphaned proof removed, because that runs internally rather than through the storage route.</p><p><strong>Size limit.</strong> The request may carry at most <strong>1</strong> file part, capped at <code>MAX_FILE_SIZE</code> (default <strong>52428800</strong> bytes = 50 MiB). Multer uses memory storage and <strong>aborts the stream at the threshold</strong> rather than buffering the whole part and rejecting it afterwards; an oversized file answers <code>413</code>. A <strong>second</strong> file part hits a different Multer limit (<code>LIMIT_FILE_COUNT</code> / <code>LIMIT_UNEXPECTED_FILE</code>) and Nest maps those to <code>400</code>, not <code>413</code> — the size cap is the only <code>413</code> here.</p><blockquote><p><strong>Every scalar arrives as a string.</strong> The global pipe runs with <code>enableImplicitConversion: false</code>, so the DTO converts each number field explicitly. Send them as ordinary form fields — <code>-F &quot;shortfallAmountSAR=500&quot;</code> — and do not quote-wrap them as JSON.</p><p><strong>A misspelled field is silently dropped, not rejected</strong> (<code>whitelist: true</code>, <code>forbidNonWhitelisted: false</code>). A typo&#39;d <code>settlementRateToSar</code> therefore does not 400 on the typo — it 400s on <code>settlementAmount and settlementRateToSAR must be provided together.</code>, which is the same error a genuinely missing rate produces. Check the spelling before assuming the pairing rule is wrong.</p></blockquote><h3 id="the-currency-contract" tabindex="-1">The currency contract <a class="header-anchor" href="#the-currency-contract" aria-label="Permalink to &quot;The currency contract&quot;">​</a></h3><p><code>shortfallAmountSAR</code> is what is <strong>owed</strong>. The three settlement fields describe what <strong>physically moved</strong>. They are separate on purpose: money can leave at an effective rate that no published rate row carries.</p><blockquote><p>⚠️ <strong>Rate direction.</strong> <code>settlementRateToSAR</code> is <strong>SAR per ONE unit of <code>settlementCurrency</code></strong>, so that <code>settlementAmount × settlementRateToSAR ≈ shortfallAmountSAR</code>.</p><p><strong>This is the inverse of the SAR→IDR rate shown elsewhere in the admin UI.</strong> That surface shows <code>1 SAR = 4166.67 IDR</code>; this field wants <code>1 IDR = 0.00024 SAR</code>. Sending <code>4166.67</code> here claims a transfer worth roughly nine billion SAR — it is recorded anyway (see below), so the mistake surfaces as <code>payoutReconciled: false</code> rather than as a rejection. Stored at 8dp (<code>Decimal(18,8)</code>) precisely to keep IDR usable at that magnitude.</p></blockquote><p><strong>SAR — the identity case. Omit both figures.</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>shortfallAmountSAR: 500,  settlementCurrency: &quot;SAR&quot;</span></span>
<span class="line"><span>→ stored settlementAmount 500, settlementRateToSAR 1, settlementReconciled true</span></span></code></pre></div><p>They are filled in as the SAR amount itself at a rate of <code>1</code>. Sending them explicitly (<code>500</code> and <code>1</code>) is accepted and identical.</p><p><strong>USD and IDR — both figures are required.</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>500 SAR owed, paid in USD at 3.75 SAR per USD</span></span>
<span class="line"><span>settlementCurrency &quot;USD&quot;, settlementAmount 133.33, settlementRateToSAR 3.75</span></span>
<span class="line"><span>→ 133.33 × 3.75 = 499.9875 SAR   |diff| 0.0125 → settlementReconciled true</span></span>
<span class="line"><span></span></span>
<span class="line"><span>500 SAR owed, paid in IDR</span></span>
<span class="line"><span>settlementCurrency &quot;IDR&quot;, settlementAmount 2083333, settlementRateToSAR 0.00024</span></span>
<span class="line"><span>→ 2083333 × 0.00024 = 499.99992 SAR   |diff| 0.00008 → settlementReconciled true</span></span></code></pre></div><p>Neither may be omitted. A bank refund can fall back on a suggestion stored when the payout was initiated; <strong>a shortfall settlement never had one</strong>, because nothing here ever computed an amount. &quot;Neither&quot; would store a null amount alongside a <code>reconciled</code> flag that means nothing.</p><p><strong>Reconciliation is recorded, never enforced.</strong></p><p>The pair is checked against the owed SAR with a tolerance of <strong>1 SAR</strong>, widened by <code>|settlementAmount| × 1e-8</code> to absorb the pure storage error introduced by back-multiplying an 8dp rate against a high-magnitude IDR figure. For SAR and USD that slack is sub-cent and effectively a no-op; real divergences stay orders of magnitude above it.</p><p>A pair that falls outside tolerance is <strong>stored</strong> with <code>payoutReconciled: false</code> and a server-side warning — it is <strong>never rejected</strong>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>500 SAR owed, settlementCurrency &quot;USD&quot;, settlementAmount 10, settlementRateToSAR 3.75</span></span>
<span class="line"><span>→ 10 × 3.75 = 37.5 SAR   |diff| 462.5  → settlementReconciled false, 201 Created</span></span></code></pre></div><blockquote><p><strong>Why a divergence is not a <code>400</code>.</strong> The money has already left. A bank fee or a rounding decision is a fact about the transfer, not grounds to refuse the record of it — refusing would leave the transfer real and unrecorded, which is strictly worse than recording it with a flag. Treat <code>payoutReconciled: false</code> as a review queue, not as a failure.</p></blockquote><p><strong>Rate provenance.</strong> For a non-SAR settlement the current rate row&#39;s id is resolved and stored as <code>settlementExchangeRateId</code> — provenance only, saying which published rate the figure was reached against. It is <strong>never read back to re-derive the amount</strong>; <code>settlementRateToSAR</code> on the row stays authoritative. If no rate row can be resolved the create still succeeds with a null pointer and a logged warning. The id is <strong>not</strong> emitted in the response.</p><blockquote><p>⛔ <strong>For IDR, the lookup deliberately asks for the <code>SAR</code> rate row, not an <code>IDR</code> one.</strong> Rates are stored keyed on <code>toCurrency</code>, and there is no <code>toCurrency = &#39;IDR&#39;</code> row — the <code>SAR</code> row <strong>is</strong> the IDR pair (its <code>sellRate</code> is IDR-per-SAR). <code>resolveRateProvenance</code> aliases <code>IDR → SAR</code> before calling <code>getCurrentRate</code>; calling <code>getCurrentRate(&#39;IDR&#39;)</code> directly would find nothing, so every IDR settlement would store a null pointer regardless of whether a current rate exists. USD is looked up as itself — only IDR gets the alias.</p></blockquote><h3 id="what-this-endpoint-deliberately-does-not-do" tabindex="-1">What this endpoint deliberately does NOT do <a class="header-anchor" href="#what-this-endpoint-deliberately-does-not-do" aria-label="Permalink to &quot;What this endpoint deliberately does NOT do&quot;">​</a></h3><blockquote><p>⛔ <strong>It suggests nothing, and the only check that compares the AMOUNT to the booking is the ceiling: it may not exceed the booking total.</strong> The remaining checks on the figure are format-level — positive, at most two decimals, within the column&#39;s range. (The route has other booking-derived rejections — status, a missing recorded count, nothing outstanding, a booking with no value — but none of them looks at the amount you sent.) There is no <code>expectedAmountSAR</code> field on this request, on this response, or on the settlement row in the database. Whatever finance types is what is recorded.</p></blockquote><p>The booking detail <em>does</em> carry a per-leg refund <strong>estimate</strong> (<code>shortfallRefundTotalSAR</code> — see <a href="/ahlaan-train-docs/admin/custom-booking/#get-custom-booking-detail">Custom Booking</a>). It exists so the customer&#39;s &quot;Berita Acara Realisasi&quot; can be printed, and this endpoint neither reads it nor compares the submitted amount to it. It could not usefully do either, because the estimate is knowingly imprecise:</p><ul><li><strong><code>ticketedPaxCount</code> is one number.</strong> It says <em>how many</em> passengers were ticketed, never <em>which ones</em>. A booking short by one passenger does not record who that passenger was.</li><li><strong>Adult and child prices differ.</strong> <code>pricePerAdultSAR</code> and <code>pricePerChildSAR</code> are separate figures, so &quot;one passenger&#39;s worth&quot; has no single value until you know which passenger was missed — which the previous point says you do not. The estimate resolves this by pricing every missing seat at the <strong>adult</strong> fare, which over-refunds a mixed booking on purpose.</li></ul><p>Treated as an authority, that figure would be a guess wearing the authority of a computed number, and a wrong guess here sends the wrong amount of real money. So finance types the figure and <code>amountReason</code> keeps their reason for it. The document the customer receives says the same thing in its own footnote — <em>&quot;Nilai final akan menyesuaikan hasil konfirmasi dari pihak Finance&quot;</em>.</p><h3 id="no-second-approver" tabindex="-1">No second approver <a class="header-anchor" href="#no-second-approver" aria-label="Permalink to &quot;No second approver&quot;">​</a></h3><blockquote><p>⚠️ <strong>Every settlement response carries <code>requiresApproval: false</code> and <code>approvedBy: null</code>.</strong> No dual-control flow exists for this record type. The admin who creates the settlement is the only human on the record.</p></blockquote><p>This matters because of where these rows are rendered. The response field names deliberately overlap the bank-refund response so <strong>one frontend row component can render a Refunds screen with a bank-refund tab and a shortfall tab</strong> — and a bank refund on the neighbouring tab genuinely does carry a second approver. On a shared screen, a settlement nobody countersigned must never be mistaken for an approved bank refund. <code>requiresApproval</code> and <code>approvedBy</code> are what tell them apart; they are emitted on every row precisely so the FE never has to infer it from which tab it happens to be on.</p><p>The <code>approvedBy</code> column exists in the database and is written as an explicit <code>null</code> rather than left unset, so a later dual-control flow needs a code change and not a migration.</p><h3 id="the-frozen-counts" tabindex="-1">The frozen counts <a class="header-anchor" href="#the-frozen-counts" aria-label="Permalink to &quot;The frozen counts&quot;">​</a></h3><p>The settlement row stores its <strong>own copy</strong> of <code>expectedTicketCount</code>, <code>ticketedPaxCount</code> and <code>unticketedPaxCount</code> rather than reading through to the booking.</p><p>This record has to keep saying what was owed <strong>on the day the money left</strong>, so it freezes the three figures at settlement time. <code>NOT NULL</code> columns, all three.</p><p>Freezing is belt-and-braces rather than the only defence: once a settlement exists the booking&#39;s <code>ticketedPaxCount</code> may no longer move at all. An upload may restate the same count to replace a bad file, but a raised or lowered count is refused on <strong>every</strong> status, including after a manager has closed the booking — see <a href="/ahlaan-train-docs/admin/custom-booking/#_5-upload-ticket">Custom Booking → §5</a>. The frozen copies survive a hand-fixed row, which that guard cannot.</p><p><code>expectedTicketCount</code> is <code>adultCount + childCount</code>. <strong>Infants are excluded</strong> — an infant takes no seat, so the ticket source issues nothing for one and nothing was ever owed for one. A 8 + 2 + 3 booking has an expected count of <strong>10</strong>, not 13.</p><h3 id="order-of-checks" tabindex="-1">Order of checks <a class="header-anchor" href="#order-of-checks" aria-label="Permalink to &quot;Order of checks&quot;">​</a></h3><p>Worth knowing when debugging a <code>400</code>, because the first failure wins:</p><ol><li><strong>Validation</strong> — the DTO, before the handler runs.</li><li><strong>Proof MIME type</strong> — in the controller, before anything reaches R2 and before the booking is even loaded. An unsupported proof on a non-existent booking answers the MIME <code>400</code>, not the <code>404</code>.</li><li><strong>Booking exists</strong> → <code>404</code>.</li><li><strong>Status is <code>TICKET_UPLOADED_PARTIAL</code></strong> → <code>400</code>.</li><li><strong>A ticketed count was recorded</strong> → <code>400</code>.</li><li><strong>Something is actually outstanding</strong> → <code>400</code>.</li><li><strong><code>shortfallAmountSAR</code> does not exceed the booking total</strong> → <code>400</code>.</li><li><strong>The currency pairing rule</strong> → <code>400</code>.</li><li>Rate provenance lookup (non-fatal), then the proof upload to R2, then the transaction: settlement row + status write + audit entry, together or not at all — plus the settlement table&#39;s own unique constraint, which turns a concurrent <strong>duplicate</strong> create into a <code>409</code> on the insert rather than a <code>500</code>, before the status compare-and-set below even runs.</li></ol><p>If the transaction fails — including on a lost race — any proof already uploaded is <strong>taken back out of storage</strong>, best-effort. An R2 call cannot take part in a database transaction, so the object goes up first and the cleanup catches it on the way down.</p><h3 id="curl" tabindex="-1">cURL <a class="header-anchor" href="#curl" aria-label="Permalink to &quot;cURL&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># SAR settlement — the identity case, both figures omitted</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> POST</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /api/v1/admin/shortfall-settlements</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Authorization: Bearer &lt;admin-token&gt;&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -F</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;customBookingId=019f6ef6-8e81-724f-9faf-7e664b9bceee&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -F</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;shortfallAmountSAR=500&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -F</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;settlementCurrency=SAR&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -F</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;reference=TRF-99&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -F</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;amountReason=1 of 10 passengers unticketed; adult fare, outbound leg&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># IDR settlement — both figures mandatory, rate is SAR per ONE IDR</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> POST</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /api/v1/admin/shortfall-settlements</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Authorization: Bearer &lt;admin-token&gt;&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -F</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;customBookingId=019f6ef6-8e81-724f-9faf-7e664b9bceee&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -F</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;shortfallAmountSAR=500&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -F</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;settlementCurrency=IDR&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -F</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;settlementAmount=2083333&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -F</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;settlementRateToSAR=0.00024&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -F</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;reference=BCA-20260806-0031&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># With the transfer proof attached</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> POST</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /api/v1/admin/shortfall-settlements</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Authorization: Bearer &lt;admin-token&gt;&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -F</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;customBookingId=019f6ef6-8e81-724f-9faf-7e664b9bceee&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -F</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;shortfallAmountSAR=500&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -F</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;settlementCurrency=SAR&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -F</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;reference=TRF-99&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -F</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;proofFile=@bukti-transfer.pdf&quot;</span></span></code></pre></div><h3 id="response-201" tabindex="-1">Response (201) <a class="header-anchor" href="#response-201" aria-label="Permalink to &quot;Response (201)&quot;">​</a></h3><p>Real capture, preprod 2026-08-07 — booking <code>UW4V4KK</code>, 10 adults at <code>totalPriceSAR</code> 2190, one passenger unticketed, settled in IDR at 912,500 IDR × 0.00024 = 219.00 SAR with a proof file attached:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;019fd9e4-31ae-711b-bbef-604c25e086e2&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;customBookingId&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;019f6ef6-8e81-724f-9faf-7e664b9bceee&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;bookingCode&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;UW4V4KK&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;settlementSequence&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;refundAmountSAR&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">219</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;bankTransferReference&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;SMOKE-564-BCA-20260807&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;processedAt&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;2026-08-07T01:43:58.638Z&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;processedBy&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;019c9105-311a-732f-a1c5-da44758a7e5b&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;status&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;COMPLETED&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;payoutCurrency&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;IDR&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;payoutAmount&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">912500</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;payoutRateToSAR&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.00024</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;payoutReconciled&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;bankProofUploaded&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;expectedTicketCount&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;ticketedPaxCount&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">9</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;unticketedPaxCount&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;amountReason&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;1 of 10 passengers not ticketed by the channel&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;settlementReconciled&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;requiresApproval&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;approvedBy&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;createdAt&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;2026-08-07T01:43:58.638Z&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>processedAt</code> and <code>createdAt</code> are identical because both are written in the same transaction. <code>processedBy</code> is the acting admin&#39;s user id.</p><p>The booking is <code>SHORTFALL_REFUNDED</code> by the time this response is written.</p><h3 id="response-fields" tabindex="-1">Response Fields <a class="header-anchor" href="#response-fields" aria-label="Permalink to &quot;Response Fields&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td><code>id</code></td><td>string</td><td>The settlement&#39;s own id. Pass it to <code>GET /:id</code>.</td></tr><tr><td><code>customBookingId</code></td><td>string</td><td>The booking settled.</td></tr><tr><td><code>bookingCode</code></td><td>string | null</td><td>Joined from the booking.</td></tr><tr><td><code>settlementSequence</code></td><td>number</td><td><code>1</code> for the first settlement on a booking. Today nothing writes a second — create is allowed only from <code>TICKET_UPLOADED_PARTIAL</code> and the first settlement moves the booking off it. The column and its unique constraint exist so a later &quot;part now, remainder when the channel refunds us&quot; flow needs no migration.</td></tr><tr><td><code>refundAmountSAR</code></td><td>number</td><td><strong><code>shortfallAmountSAR</code> under a refund-shaped name.</strong> The canonical owed figure.</td></tr><tr><td><code>bankTransferReference</code></td><td>string</td><td><strong>The <code>reference</code> field, refund-shaped.</strong></td></tr><tr><td><code>processedAt</code></td><td>date</td><td><strong><code>settledAt</code>, refund-shaped.</strong></td></tr><tr><td><code>processedBy</code></td><td>string</td><td><strong><code>settledBy</code>, refund-shaped</strong> — the admin user id.</td></tr><tr><td><code>status</code></td><td>string</td><td>Always the literal <code>&quot;COMPLETED&quot;</code>. A settlement has no lifecycle: it exists because the money already moved.</td></tr><tr><td><code>payoutCurrency</code></td><td>string</td><td><code>settlementCurrency</code>.</td></tr><tr><td><code>payoutAmount</code></td><td>number</td><td><code>settlementAmount</code>, falling back to the SAR figure so a SAR row never shows a <code>null</code> where the refund tab shows a number.</td></tr><tr><td><code>payoutRateToSAR</code></td><td>number</td><td><code>settlementRateToSAR</code>, falling back to <code>1</code> for the same reason.</td></tr><tr><td><code>payoutReconciled</code></td><td>boolean</td><td><code>settlementReconciled</code>. <code>false</code> means the pair did not back-convert within tolerance — recorded, not rejected.</td></tr><tr><td><code>bankProofUploaded</code></td><td>boolean</td><td>Whether a proof file is attached. <strong>The file id itself is not emitted</strong>, so this response cannot be used to fetch the proof.</td></tr><tr><td><code>expectedTicketCount</code></td><td>number</td><td>Frozen. <code>adultCount + childCount</code> at settlement time.</td></tr><tr><td><code>ticketedPaxCount</code></td><td>number</td><td>Frozen.</td></tr><tr><td><code>unticketedPaxCount</code></td><td>number</td><td>Frozen. <code>expected − ticketed</code>.</td></tr><tr><td><code>amountReason</code></td><td>string | null</td><td>Finance&#39;s explanation of the figure.</td></tr><tr><td><code>settlementReconciled</code></td><td>boolean</td><td>Same value as <code>payoutReconciled</code>, under the native name. Both are emitted.</td></tr><tr><td><code>requiresApproval</code></td><td><code>false</code></td><td>Always. No dual control exists for this record type.</td></tr><tr><td><code>approvedBy</code></td><td>null</td><td>Always, today. The column exists for a future dual-control flow.</td></tr><tr><td><code>createdAt</code></td><td>date</td><td>Row creation. Distinct from <code>processedAt</code> only in principle — both are written in the same transaction.</td></tr></tbody></table><blockquote><p><strong>The refund-shaped names are deliberate, and the columns underneath are named differently.</strong> The database columns are <code>shortfallAmountSAR</code>, <code>reference</code>, <code>settledAt</code>, <code>settledBy</code>, <code>settlementCurrency</code>, <code>settlementAmount</code>, <code>settlementRateToSAR</code>, <code>settlementReconciled</code>, <code>proofFileId</code>. The response renames them to the refund vocabulary so <strong>one frontend row component renders both tabs</strong>. Do not &quot;fix&quot; the mapping to match the schema — the divergence is the feature.</p></blockquote><p><strong>Not emitted:</strong> <code>proofFileId</code>, <code>settlementExchangeRateId</code>, <code>updatedAt</code>, and — as <a href="#what-this-endpoint-deliberately-does-not-do">above</a> — any expected-amount field. The booking detail&#39;s estimate is not repeated here, so nothing on this response can be mistaken for a figure the settled amount was checked against.</p><h3 id="side-effect-—-audit-log" tabindex="-1">Side Effect — Audit log <a class="header-anchor" href="#side-effect-—-audit-log" aria-label="Permalink to &quot;Side Effect — Audit log&quot;">​</a></h3><p>Every settlement writes an audit entry <strong>inside the same transaction</strong> as the settlement row and the status write, so the record of the money and the record of who moved it commit together or not at all.</p><p><code>action: CUSTOM_BOOKING_SHORTFALL_SETTLED</code>, <code>entity: CustomBooking</code>, <code>entityId</code> = the booking id, with a <code>newValue</code> carrying <code>settlementId</code>, <code>settlementSequence</code>, <code>shortfallAmountSAR</code>, <code>settlementCurrency</code>, <code>settlementAmount</code>, <code>settlementRateToSAR</code>, <code>settlementReconciled</code>, <code>expectedTicketCount</code>, <code>ticketedPaxCount</code>, <code>unticketedPaxCount</code>, <code>reference</code>, <code>proofFileId</code>, <code>statusBefore</code> and <code>statusAfter</code>.</p><p>Unlike the ticket upload&#39;s audit — written after the fact and best-effort, because that path holds an R2 call open — a failure here rolls the whole settlement back.</p><h3 id="errors" tabindex="-1">Errors <a class="header-anchor" href="#errors" aria-label="Permalink to &quot;Errors&quot;">​</a></h3><p>Verbatim messages, with the interpolated values shown for <strong>a 10-seated-passenger booking with 9 ticketed</strong> (<code>ABC1234</code>, one passenger short):</p><ul><li><code>403 Forbidden</code> — fires <strong>before</strong> every message below, because the permission guard runs ahead of the handler. The caller is not an <code>ADMIN</code>, or the role lacks <code>refunds:shortfall-settlement</code>. Body: <code>{&quot;message&quot;:&quot;Forbidden resource&quot;,&quot;error&quot;:&quot;Forbidden&quot;,&quot;statusCode&quot;:403}</code>. ⚠️ A role holding only <code>*:read</code> gets this on create while both <code>GET</code>s succeed — the read-only wildcard confers <code>refunds:read</code> and nothing else.</li><li><code>400 Bad Request</code> — validation, raised by the pipe before the handler: <code>customBookingId must be a UUID</code>, <code>shortfallAmountSAR must not be less than 0.01</code>, <code>shortfallAmountSAR must not be greater than 9999999999999.99</code>, <code>shortfallAmountSAR must be a number conforming to the specified constraints</code> (more than 2 decimal places), <code>settlementCurrency must be one of the following values: SAR, USD, IDR</code>, <code>reference should not be empty</code>, <code>reference must be shorter than or equal to 255 characters</code>, <code>amountReason must be shorter than or equal to 2000 characters</code>. ⚠️ <strong>Not <code>must be a positive number</code> any more.</strong> The field dropped <code>@IsPositive</code> for a <code>@Min(0.01)</code> / <code>@Max(9999999999999.99)</code> pair — the DECIMAL column&#39;s ceiling, so an out-of-range insert 400s at the DTO instead of 500-ing on a numeric overflow inside the transaction. <code>settlementAmount</code> carries the same pair; <code>settlementRateToSAR</code> carries its own (<code>@Min(0.000001)</code> / <code>@Max(9999999999)</code>), each producing the matching <code>must not be less/greater than &lt;bound&gt;</code> message for that field.</li><li><code>400 Bad Request</code> — <code>Unsupported proof type &quot;application/zip&quot;. Allowed: application/pdf, image/jpeg, image/png.</code> Raised by the controller before the file reaches storage and before the booking is loaded; the rejected type is echoed back.</li><li><code>400 Bad Request</code> — <code>Cannot settle a shortfall when booking status is &quot;PAID&quot;. Only TICKET_UPLOADED_PARTIAL has a shortfall outstanding.</code> The quoted status is whatever the booking actually holds. Every non-partial status hits this, including <code>SHORTFALL_REFUNDED</code> itself — a second settlement against an already-settled booking is refused here.</li><li><code>400 Bad Request</code> — <code>Booking ABC1234 is TICKET_UPLOADED_PARTIAL but has no recorded ticketed count, so there is no shortfall to size a settlement against.</code> Defensive: only an upload writes that status and an upload always records a count, but the frozen columns are <code>NOT NULL</code> and this row is the basis of a refund, so it verifies rather than assumes.</li><li><code>400 Bad Request</code> — <code>Booking ABC1234 has all 10 passenger(s) ticketed, so nothing is owed back.</code> Fires when the recorded count has caught up with the expected count.</li><li><code>400 Bad Request</code> — <code>Booking ABC1234 has no recorded value, so there is nothing for a shortfall to return part of.</code> A booking whose <code>totalPriceSAR</code> is not a finite positive number is refused outright rather than having the ceiling skipped — skipping would leave the column maximum as the only bound on the figure.</li><li><code>400 Bad Request</code> — <code>shortfallAmountSAR (600) exceeds what was paid for this booking. A shortfall returns part of what was paid, so it cannot be larger than the whole booking.</code> Only the figure you sent is interpolated. <strong>Neither message names the booking total</strong>, deliberately: <code>totalPriceSAR</code> is stripped from every custom-booking response unless the caller holds <code>custom-bookings:read-pricing</code>, and this route is reachable with <code>refunds:shortfall-settlement</code> alone — quoting it back in a rejection would hand it over in a single request. <strong>A sanity ceiling, not an expectation</strong> — it isn&#39;t sizing the shortfall. Nothing else compares the figure to the booking; the remaining checks are format-level (positive, at most two decimals, within the column&#39;s range). A shortfall only ever returns part of what the customer already paid, so the figure can never exceed the booking total regardless of which passenger or leg was missed.</li><li><code>400 Bad Request</code> — <code>settlementAmount and settlementRateToSAR must be provided together.</code> One without the other, in either direction, in any currency.</li><li><code>400 Bad Request</code> — <code>settlementAmount and settlementRateToSAR are required for a IDR settlement. Only a SAR settlement can omit them, where the amount is the SAR figure itself at a rate of 1.</code> The currency name is interpolated (<code>USD</code> reads the same way).</li><li><code>404 Not Found</code> — <code>Custom booking not found</code>.</li><li><code>409 Conflict</code> — <strong>two distinct causes</strong>, both translated from what would otherwise be a raw <code>500</code>: <ul><li><code>Cannot settle: booking ABC1234 moved while this request was in flight. Both its status and its recorded ticketed count are checked, so re-read the booking before retrying — the status alone may look unchanged.</code> The status write is pinned to <strong>both</strong> the status and the recorded ticketed count. ⚠️ <strong>A concurrent ticket upload lands here too</strong>: a late top-up moves the booking to <code>TICKET_UPLOADED_FULL</code> and rewrites <code>ticketedPaxCount</code>, and a status-only pin would still match a row whose count had gone from 9 to 10 — freezing a stale <code>unticketedPaxCount</code> into a record claiming more money than was ever owed. So a booking that <em>looks</em> unchanged can still lose the race.</li><li><code>A settlement for booking 019f6ef6-8e81-724f-9faf-7e664b9bceee was recorded by another request. Re-read the booking before retrying — its shortfall may already have been settled.</code> A <strong>concurrent duplicate create</strong> — two requests racing to settle the same booking. This one fires on the settlement table&#39;s unique constraint at <strong>insert time</strong>, before the status compare-and-set above ever runs, so it pre-empts the &quot;booking moved&quot; conflict rather than compounding it. Note the message quotes the raw <code>customBookingId</code> (the UUID), not the <code>bookingCode</code> the other 409 uses — a deliberate reminder that this is a different code path, not the same message with different values. Either way, any proof already uploaded is taken back out of storage.</li></ul></li><li><code>413 Payload Too Large</code> — the proof file exceeded <code>MAX_FILE_SIZE</code> (default <strong>52428800</strong> bytes). Aborted mid-stream by multer rather than buffered and rejected — the same status the edge proxy returns for an oversized body, so the app and the edge agree on the answer. <strong>Size only</strong> — sending more than 1 file part is a different Multer limit (<code>LIMIT_FILE_COUNT</code> / <code>LIMIT_UNEXPECTED_FILE</code>), which Nest maps to <code>400 Bad Request</code>, not <code>413</code>.</li></ul><hr><h2 id="_2-list-shortfall-settlements" tabindex="-1">2. List Shortfall Settlements <a class="header-anchor" href="#_2-list-shortfall-settlements" aria-label="Permalink to &quot;2. List Shortfall Settlements&quot;">​</a></h2><div class="language-http vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">http</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">GET</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /api/v1/admin/shortfall-settlements</span></span></code></pre></div><p>Paginated, <strong>newest settlement first</strong>. Throttled at 60 req / 60s. Needs <code>refunds:read</code>.</p><h3 id="query-parameters" tabindex="-1">Query Parameters <a class="header-anchor" href="#query-parameters" aria-label="Permalink to &quot;Query Parameters&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Param</th><th>Type</th><th>Default</th><th>Notes</th></tr></thead><tbody><tr><td><code>page</code></td><td>integer</td><td><code>1</code></td><td>Min 1.</td></tr><tr><td><code>limit</code></td><td>integer</td><td><code>20</code></td><td>Min 1, <strong>max 100</strong> — inherited from the shared pagination DTO. The refund module&#39;s own list DTO does not inherit it and can be asked for an unbounded page; this one cannot.</td></tr><tr><td><code>customBookingId</code></td><td>uuid</td><td>—</td><td>Only settlements against this booking.</td></tr><tr><td><code>bookingCode</code></td><td>string</td><td>—</td><td>Only settlements against the booking with this code. <strong>Exact match</strong>, max 32 chars.</td></tr><tr><td><code>sortBy</code></td><td>enum</td><td><code>settledAt</code></td><td><code>settledAt</code> or <code>createdAt</code>.</td></tr><tr><td><code>sortOrder</code></td><td>enum</td><td><code>desc</code></td><td><code>asc</code> or <code>desc</code>.</td></tr></tbody></table><blockquote><p><strong>There is deliberately no customer or organization filter.</strong> A settlement hangs off the <strong>booking</strong>, not off a user, and the row carries neither a user id nor an organization id — adding those filters would mean joining through the booking to answer a question the record does not model. Filter by <code>customBookingId</code> or <code>bookingCode</code> instead; get the booking ids from <a href="/ahlaan-train-docs/admin/custom-booking/#list-custom-bookings">the custom-booking list</a>, which does filter by customer and organization.</p></blockquote><h3 id="curl-1" tabindex="-1">cURL <a class="header-anchor" href="#curl-1" aria-label="Permalink to &quot;cURL&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> GET</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;/api/v1/admin/shortfall-settlements?bookingCode=ABC1234&amp;limit=20&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Authorization: Bearer &lt;admin-token&gt;&quot;</span></span></code></pre></div><h3 id="response-200" tabindex="-1">Response (200) <a class="header-anchor" href="#response-200" aria-label="Permalink to &quot;Response (200)&quot;">​</a></h3><p>Real capture, preprod 2026-08-07, <code>?page=1&amp;limit=20</code>:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;data&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;019fd9e4-31ae-711b-bbef-604c25e086e2&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;customBookingId&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;019f6ef6-8e81-724f-9faf-7e664b9bceee&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;bookingCode&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;UW4V4KK&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;settlementSequence&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;refundAmountSAR&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">219</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;bankTransferReference&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;SMOKE-564-BCA-20260807&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;processedAt&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;2026-08-07T01:43:58.638Z&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;processedBy&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;019c9105-311a-732f-a1c5-da44758a7e5b&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;status&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;COMPLETED&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;payoutCurrency&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;IDR&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;payoutAmount&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">912500</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;payoutRateToSAR&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.00024</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;payoutReconciled&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;bankProofUploaded&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;expectedTicketCount&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;ticketedPaxCount&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">9</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;unticketedPaxCount&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;amountReason&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;1 of 10 passengers not ticketed by the channel&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;settlementReconciled&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;requiresApproval&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;approvedBy&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;createdAt&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;2026-08-07T01:43:58.638Z&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;meta&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;total&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;page&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;limit&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">20</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;totalPages&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Each row is the <strong>same shape</strong> as the create response, field for field — verified live by comparing this row against <code>GET :id</code> for the same settlement: the two bodies were byte-identical.</p><blockquote><p><strong>The envelope is <code>{ data, meta }</code></strong>, matching the rest of the admin API rather than the refund list&#39;s flat shape. <code>meta.totalPages</code> is guarded against a zero <code>limit</code> and reads <code>0</code> rather than <code>Infinity</code> — the refund list divides unconditionally.</p></blockquote><h3 id="errors-1" tabindex="-1">Errors <a class="header-anchor" href="#errors-1" aria-label="Permalink to &quot;Errors&quot;">​</a></h3><ul><li><code>403 Forbidden</code> — not an <code>ADMIN</code>, or the role lacks <code>refunds:read</code>.</li><li><code>400 Bad Request</code> — validation on the query params (a non-UUID <code>customBookingId</code>, a <code>limit</code> above 100, an unrecognised <code>sortBy</code>).</li></ul><hr><h2 id="_3-get-a-shortfall-settlement" tabindex="-1">3. Get a Shortfall Settlement <a class="header-anchor" href="#_3-get-a-shortfall-settlement" aria-label="Permalink to &quot;3. Get a Shortfall Settlement&quot;">​</a></h2><div class="language-http vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">http</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">GET</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /api/v1/admin/shortfall-settlements/:id</span></span></code></pre></div><p>Returns <strong>the same shape as one list row</strong>, matching how the refund module already works. Throttled at 60 req / 60s. Needs <code>refunds:read</code>.</p><h3 id="curl-2" tabindex="-1">cURL <a class="header-anchor" href="#curl-2" aria-label="Permalink to &quot;cURL&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> GET</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /api/v1/admin/shortfall-settlements/019fd9e4-31ae-711b-bbef-604c25e086e2</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Authorization: Bearer &lt;admin-token&gt;&quot;</span></span></code></pre></div><h3 id="errors-2" tabindex="-1">Errors <a class="header-anchor" href="#errors-2" aria-label="Permalink to &quot;Errors&quot;">​</a></h3><ul><li><code>403 Forbidden</code> — not an <code>ADMIN</code>, or the role lacks <code>refunds:read</code>.</li><li><code>404 Not Found</code> — <code>Shortfall settlement not found</code>.</li></ul><hr><h2 id="notes-for-frontend" tabindex="-1">Notes for frontend <a class="header-anchor" href="#notes-for-frontend" aria-label="Permalink to &quot;Notes for frontend&quot;">​</a></h2><ul><li><strong>Money fields are not gated here.</strong> The custom-booking routes hide their selling prices behind <code>custom-bookings:read-pricing</code> and their cost basis behind <code>custom-bookings:read-capital-markup</code>; neither is applied to any route on this page. Money owed <strong>back</strong> to a customer is not margin data, and these responses carry no selling price, cost or capital figure.</li><li><strong><code>payoutReconciled: false</code> is a review signal, not an error.</strong> The settlement is valid and the booking has moved. Surface it; do not retry it.</li><li><strong><code>requiresApproval</code> and <code>approvedBy</code> are the only things distinguishing a settlement from an approved bank refund</strong> in a shared row component. Render them.</li><li><strong><code>bankProofUploaded</code> is a boolean with no file id behind it.</strong> There is no download link to build from this response.</li><li><strong>After a settlement the booking is <code>SHORTFALL_REFUNDED</code></strong>, from which the only moves are <code>COMPLETED</code> (a verification step needing <code>custom-bookings:complete</code>; it emails nobody, and neither does the settlement itself — the customer was mailed their tickets when the first ones were uploaded) or <code>CANCELLED</code>. A re-upload against a settled booking may restate the same count to replace a bad file but may not change it in either direction — see <a href="/ahlaan-train-docs/admin/custom-booking/#_5-upload-ticket">Custom Booking → §5</a>.</li></ul>`,105)])])}const u=t(o,[["render",n]]);export{k as __pageData,u as default};
